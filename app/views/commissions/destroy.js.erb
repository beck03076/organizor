<% if !@registration.nil? %>
info("Commission Delete","Deleted successfully!");

$('.bClose').trigger("click");

$("#all_payments").html("<%= escape_javascript(render :partial => 'registrations/payment') %>");

<% else %>
info("Commission Delete","You can only delete the last payment!");
<% end %>
