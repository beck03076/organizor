<%= f.fields_for :contracts do |c| %>

    <% out = prep_pre(c) %>

    <% [[:prohibited_countries,"token_coun_pro"], 
        [:permitted_countries,"token_coun_per"],
        [:prohibited_regions,"token_reg_pro"],
        [:permitted_regions,"token_reg_per"]].each do |el| %>
      <%= bs(c,[el[0],class: el[1],
                data: { pre: out[("all_" + el[0].to_s).to_sym] } ],"text_field",[nil,4],7,6) %> 
    <% end %>  

<% end %>
