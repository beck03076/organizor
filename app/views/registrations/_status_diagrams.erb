<!-- named as status diagram because in future it is going to be a diagram -->

     

               <% u = sd.user %>
               <br/>
               <%= image_tag((u.image? ? u.image_url : "/images/icons/user.png"),
                                   {width: "40",
                                    class: "imgb fl",
                                    style: "margin-right:10px;"}) rescue "User Deleted!" %>
               <span style="padding-top:10px;padding-right:100px;">
                    <%= (sd.status_name || sd.application_status.name) rescue "Unknown Status" %>
                   <br/>
                   <span class="lil">
                    at <%= sd.created_at.to_s %>
                   </span>
                   <br/>
                   <%= link_to u.name,u  rescue "User Deleted!" %> 
               
               </span>
               <div class="brg0"></div>

      

