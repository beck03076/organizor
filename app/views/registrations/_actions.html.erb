
        <% %w(Email Follow\ up Note Todo Assign\ to).each do |elem| %>
          <!--  Shortening variable names -->
          <% e = elem.downcase %>
          <%= radio_button_tag 'action',
                               e,
                               false,
                               onclick: "action_partial('registrations',
                                                        '#{e.tr(" ", "_")}',
                                                        '#{@registration.id}');" %>
          <%= label_tag elem.titleize %>
          &nbsp;&nbsp;&nbsp;&nbsp;
        <% end %>
<br/>
<br/>

