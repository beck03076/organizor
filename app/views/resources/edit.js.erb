    <% ctnt = (render 'shared/resource_form', {new_resource: @r,
                                            resource_disp1: @r_name.titleize}) %>

  $('#tester').html("<%= escape_javascript(render :partial => 'shared/modal',
                                           :locals =>   {id: 'resource-edit', 
                                                         content: ctnt.html_safe,
                                                         header: 'Edit',
                                                         button: nil}) %>");
  
  $('#resource-edit').modal('show');

