bootbox.dialog({title: "Qualification",
  message: '<%= escape_javascript(render 'table') %>',
   buttons: {
      success: {
          label: "OK",
          className: "btn-success"
      } 
   }
   });
