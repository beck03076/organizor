<%= form_for(@programme,remote: true,html: {class: "form-inline"}) do |p_fee_sta| %>
      <%= p_fee_sta.collection_select(:p_fee_status_id,ProcessingFeeStatus.order(:name),:id,:name,{},{class: "form-control"}) %>
      <%= p_fee_sta.submit "Change", {class: "form-group btn-sm btn-primary",name: "change_p_fee_status"} %>
<% end %>
