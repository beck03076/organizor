<div class="row">
    <div class="col-xs-12 text-center">
        <h3><%= @model.name %></h3>
        <%= image_tag((@model.image? ? @model.image_url : "/images/icons/user.png"),{width: "450"}) %>
    </div>
</div
